class Main 
{
    function void main() {
        var Game game;
		var Block block;
		var BlockMatrix matrix;
				
        let game = Game.new(20, 200, 12345);
		do game.run();
		do game.dispose();

        return;
    }
}
